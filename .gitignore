*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
# Per-tenant storage roots. stancl's filesystem bootstrapper suffixes
# storage_path() during tenancy, so each clinic's uploads AND its encrypted
# backup archives land here. Committing one would put a clinic's database dump
# in the repository — a `git add -A` away.
/storage/tenant*/
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
